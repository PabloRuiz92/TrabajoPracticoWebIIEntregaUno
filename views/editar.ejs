<!------------------------------------- HEADER ---------------------------------------->
<%- include('./header'); %>     
<!------------------------------------- NAVBAR ---------------------------------------->
<%- include('./navbar'); %>   
<!------------------------------------- BODY ------------------------------------------> 
<section class="container text-center my-3">
    <h1>Editar una película</h1>
    <form action="/peliculas/editar" method="POST">
    
        <h5 class="text-start">Datos actuales de la película</h5>
        <div class="row mb-3">
        <div class="col-md-12">
            <select class="form-select" name="tituloViejo" required>
                <option value="">Título actual</option>
                <% titulos.forEach(item => { %>
                  <option value="<%= item.titulo %>"><%= item.titulo %></option>
                <% }); %>
            </select>
        </div>
        </div>
    
        <h5 class="text-start">Datos nuevos de la película</h5>
        <div class="row mb-3">
        <div class="col-md-4">
            <input
            type="text"
            class="form-control"
            placeholder="Nuevo título"
            name="tituloNuevo"
            required
            />
        </div>
        <div class="col-md-4">
            <input
            type="number"
            min="1900"
            max="2099"
            class="form-control"
            placeholder="Nuevo año"
            name="anioNuevo"
            required
            />
        </div>
        <div class="col-md-4">
            <select class="form-select" name="origenNuevo" required>
            <option value="">Origen nuevo</option>
            <option value="1">Nacional</option>
            <option value="2">Internacional</option>
            </select>
        </div>
        </div>
    
        <h6 class="text-start text-muted">Todos los campos son obligatorios</h6>
    
        <div class="d-grid">
        <button type="submit" class="btn btn-block botonForm">Editar</button>
        </div>
    </form>
</section>
    
<% if (mensaje) { %>
<p class="text-center"><%= mensaje %></p>
<% } %>
<!------------------------------------- FOOTER ------------------------------------------>
<%- include('./footer'); %>